summary(mtcars)
mean(mtcars$mpg)
sd(mtcars$mpg)
max(mtcars$mpg)
min(mtcars$mpg)
quantile(mtcars$mpg, 0,5)
boxplot(mtcars$mpg,
        main = "nyoba branch baru",
        ylab = "gatau",
        xlab = "iya",
        col = "pink")
